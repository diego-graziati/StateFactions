name: StateFactions
version: '${version}'
main: it.sersapessi.sf.StateFactions
api-version: "1.20"
author: SerSapessi

commands:
  sf:
    description: State Factions plugin's command prefix.
    usage: /<command>